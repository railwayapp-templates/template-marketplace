# Deploy lobehub on Railway

Deploy and Host Lobehub. Lobechat lobe hub lobe chat

[![Deploy on Railway](https://railway.com/button.svg)](https://railway.com/deploy/lobehub)

## About

LobeHub is an open-source AI workspace for creating, organizing, and operating AI agents. It provides multi-provider model support, persistent conversations, file uploads, knowledge features, agent tools, authentication, and collaborative workflows through a polished web interface that can be self-hosted under your own Railway project and domain.

This Railway template deploys a complete server-side LobeHub stack rather than a stateless web container. It includes the LobeHub application, ParadeDB for PostgreSQL-compatible persistent data and search capabilities, Redis for caching and coordination, an S3-compatible Railway Storage Bucket for uploaded files, and persistent volumes for database, Redis, and authentication key data. Services communicate through Railway private networking whenever possible. Database migrations run automatically when LobeHub starts, while deployment-time passwords and encryption secrets are generated with Railway template functions. You only need to configure an AI provider key or connect a compatible model endpoint after deployment.

## What gets deployed

| Service | Source | Type |
|---------|--------|------|
| lobehub-railway | [XavTo/lobehub-railway](https://github.com/XavTo/lobehub-railway) | Web service |
| Redis | `redis:8.2.1` | Database |
| paradedb | `paradedb/paradedb:latest` | Database |

## Environment variables

| Variable | Service | Default | Description |
| --------- | ------- | ------- | ----------- |
| `PORT` | lobehub-railway | 3210 | Public web service port |
| `APP_URL` | lobehub-railway | - | Public URL generated by Railway |
| `REDIS_TLS` | lobehub-railway | 0 | Disables TLS for the private Redis connection |
| `REDIS_URL` | lobehub-railway | - | Private connection URL from the Redis service |
| `S3_BUCKET` | lobehub-railway | - | Name of the Railway S3 bucket |
| `S3_REGION` | lobehub-railway | - | Region of the Railway S3 bucket |
| `S3_SET_ACL` | lobehub-railway | 0 | Prevents unsupported S3 ACL operations |
| `AUTH_SECRET` | lobehub-railway | (secret) | Secret used to encrypt authentication sessions |
| `S3_ENDPOINT` | lobehub-railway | - | S3-compatible API endpoint |
| `DATABASE_URL` | lobehub-railway | - | Private PostgreSQL connection URL |
| `REDIS_PREFIX` | lobehub-railway | lobehub | Prefix used to isolate LobeHub Redis keys |
| `JWKS_DATA_DIR` | lobehub-railway | /data | Persistent directory containing the generated JWKS |
| `DATABASE_DRIVER` | lobehub-railway | node | Enables PostgreSQL support and database migrations |
| `INTERNAL_APP_URL` | lobehub-railway | http://localhost:3210 | Internal URL used for server-to-server calls |
| `S3_ACCESS_KEY_ID` | lobehub-railway | - | Access key ID for the S3 bucket |
| `KEY_VAULTS_SECRET` | lobehub-railway | (secret) | Secret used to encrypt stored API credentials |
| `S3_ENABLE_PATH_STYLE` | lobehub-railway | 0 | Uses virtual-hosted-style S3 URLs |
| `S3_SECRET_ACCESS_KEY` | lobehub-railway | (secret) | Secret access key for the S3 bucket |
| `REDISHOST` | Redis | - | Private Railway hostname of Redis |
| `REDISPORT` | Redis | 6379 | Internal Redis service port |
| `REDISUSER` | Redis | default | Default Redis ACL username |
| `REDIS_URL` | Redis | - | Private authenticated Redis URL |
| `REDISPASSWORD` | Redis | (secret) | Compatibility alias for the Redis password |
| `REDIS_PASSWORD` | Redis | (secret) | Random password generated during deployment |
| `REDIS_PUBLIC_URL` | Redis | - | Public Redis URL through the TCP proxy |
| `POSTGRES_DB` | paradedb | paradedb | Database created during initial startup |
| `DATABASE_URL` | paradedb | - | Private PostgreSQL connection URL |
| `POSTGRES_USER` | paradedb | (secret) | PostgreSQL administrator username |
| `POSTGRES_PASSWORD` | paradedb | (secret) | Random database password generated at deployment |
| `DATABASE_PUBLIC_URL` | paradedb | - | Public PostgreSQL URL through the TCP proxy |

## Configuration

- **Networking:** Public domain with automatic HTTPS
- **Volume:** `/data`
- **Start command:** `/bin/sh -c "rm -rf $RAILWAY_VOLUME_MOUNT_PATH/lost+found/ && exec docker-entrypoint.sh redis-server --requirepass $REDIS_PASSWORD --save 60 1 --dir $RAILWAY_VOLUME_MOUNT_PATH"`
- **Volume:** `/var/lib/postgresql/data`

**Category:** AI/ML · **Languages:** JavaScript, Dockerfile

[View on Railway →](https://railway.com/deploy/lobehub)
