# Deploy Akaunting on Railway

Open-source accounting with MariaDB and persistent business data.

[![Deploy on Railway](https://railway.com/button.svg)](https://railway.com/deploy/akaunting)

## About

This template runs Akaunting 3.1.21 with MariaDB 11.8.2 and a Railway cron
service for scheduled accounting tasks. Railway provides the public HTTPS
endpoint for Akaunting while MariaDB remains on private networking. Akaunting
and MariaDB each use a persistent volume, preserving the installed application,
uploads, modules, configuration, and database. Shared credentials are generated
once and passed through Railway service references. The deployer supplies only
the initial administrator email and password.

## What gets deployed

| Service | Source | Type |
|---------|--------|------|
| Scheduler | `akaunting/akaunting:3.1.21-v@sha256:0289239e38f5b21efafb152ae8d828735d38fed8477d4fb277d472f8ea3fde09` | Worker |
| Akaunting | `akaunting/akaunting:3.1.21-v@sha256:0289239e38f5b21efafb152ae8d828735d38fed8477d4fb277d472f8ea3fde09` | Web service |
| MariaDB | `mariadb:11.8.2@sha256:2bcbaec92bd9d4f6591bc8103d3a8e6d0512ee2235506e47a2e129d190444405` | Database |

## Environment variables

| Variable | Service | Default | Description |
| --------- | ------- | ------- | ----------- |
| `TZ` | Scheduler | Etc/UTC | Deterministic scheduled task timezone. |
| `APP_ENV` | Scheduler | production | Matches production application behavior. |
| `APP_KEY` | Scheduler | - | Uses the one authoritative application key. |
| `APP_URL` | Scheduler | - | Uses the same canonical public origin as Akaunting. |
| `DB_HOST` | Scheduler | - | MariaDB hostname on Railway private networking. |
| `DB_PORT` | Scheduler | 3306 | MariaDB listener port. |
| `APP_NAME` | Scheduler | Akaunting | Matches the web service application name. |
| `APP_DEBUG` | Scheduler | false | Keeps scheduled task failures sanitized. |
| `DB_PREFIX` | Scheduler | ak_ | Matches the web service table prefix. |
| `APP_LOCALE` | Scheduler | en-US | Matches the web service locale. |
| `DB_DATABASE` | Scheduler | - | References the authoritative database name. |
| `DB_PASSWORD` | Scheduler | (secret) | References the authoritative database password. |
| `DB_USERNAME` | Scheduler | (secret) | References the authoritative database user. |
| `LOG_CHANNEL` | Scheduler | stderr | Sends scheduled task logs to Railway. |
| `CACHE_DRIVER` | Scheduler | file | Supported cache driver for the ephemeral scheduled task. |
| `DB_CONNECTION` | Scheduler | mysql | Selects the MariaDB-compatible driver. |
| `SESSION_DRIVER` | Scheduler | file | Supported session driver; no user sessions run here. |
| `FIREWALL_ENABLED` | Scheduler | true | Matches Akaunting runtime configuration. |
| `QUEUE_CONNECTION` | Scheduler | sync | Runs scheduled queued work inline. |
| `MODEL_CACHE_ENABLED` | Scheduler | true | Matches Akaunting model configuration. |
| `TZ` | Akaunting | Etc/UTC | Deterministic application timezone before user configuration. |
| `PORT` | Akaunting | 80 | Railway routing port for Apache. |
| `APP_ENV` | Akaunting | production | Enables production application behavior. |
| `APP_KEY` | Akaunting | - | Fresh application encryption and signing key; referenced by Scheduler. |
| `APP_URL` | Akaunting | - | Canonical HTTPS origin generated by Railway. |
| `DB_HOST` | Akaunting | - | MariaDB hostname on Railway private networking. |
| `DB_NAME` | Akaunting | - | Installer database name from MariaDB. |
| `DB_PORT` | Akaunting | 3306 | MariaDB listener port. |
| `APP_NAME` | Akaunting | Akaunting | Application name shown in generated configuration. |
| `APP_DEBUG` | Akaunting | false | Prevents debug detail from being exposed publicly. |
| `DB_PREFIX` | Akaunting | ak_ | Akaunting table prefix used by installation and runtime. |
| `APP_LOCALE` | Akaunting | en-US | Initial application and installer locale. |
| `ADMIN_EMAIL` | Akaunting | - | Initial administrator email; required to sign in and recover the account. |
| `DB_DATABASE` | Akaunting | - | Runtime database name from MariaDB. |
| `DB_PASSWORD` | Akaunting | (secret) | References the one authoritative database password. |
| `DB_USERNAME` | Akaunting | (secret) | Runtime database user from MariaDB. |
| `LOG_CHANNEL` | Akaunting | stderr | Sends runtime logs to Railway. |
| `CACHE_DRIVER` | Akaunting | file | Uses the persistent application volume for cache state. |
| `COMPANY_NAME` | Akaunting | My Company | Initial company display name; editable after deployment. |
| `COMPANY_EMAIL` | Akaunting | - | Uses the initial administrator email for the first company. |
| `DB_CONNECTION` | Akaunting | mysql | Selects Akaunting's MySQL and MariaDB driver. |
| `ADMIN_PASSWORD` | Akaunting | (secret) | Initial administrator password; choose a unique strong value. |
| `SESSION_DRIVER` | Akaunting | file | Uses the persistent application volume for sessions. |
| `FIREWALL_ENABLED` | Akaunting | true | Enables Akaunting's supported firewall middleware. |
| `QUEUE_CONNECTION` | Akaunting | sync | Runs queued jobs inline without Redis or a worker. |
| `MODEL_CACHE_ENABLED` | Akaunting | true | Enables Akaunting model metadata caching. |
| `TZ` | MariaDB | Etc/UTC | Deterministic database timezone. |
| `MARIADB_USER` | MariaDB | (secret) | Creates the dedicated Akaunting database user. |
| `MARIADB_DATABASE` | MariaDB | akaunting | Creates the dedicated Akaunting database. |
| `MARIADB_PASSWORD` | MariaDB | (secret) | Authoritative generated application database password referenced by both PHP services. |
| `MARIADB_ROOT_PASSWORD` | MariaDB | (secret) | Independent generated MariaDB root password. |

## Configuration

- **Start command:** `/bin/bash -lc 'set -e
if [ ! -f /var/www/html/index.php ]; then
  unzip -q /tmp/akaunting.zip -d /var/www/html
fi
php artisan schedule:run --no-interaction'`
- **Start command:** `/bin/bash -lc 'set -e
if [ ! -f /var/www/html/index.php ]; then
  unzip -q /tmp/akaunting.zip -d /var/www/html
fi
a2dismod mpm_event mpm_worker >/dev/null 2>&1 || true
a2enmod mpm_prefork rewrite >/dev/null
mkdir -p /var/www/html/storage/framework/sessions /var/www/html/storage/framework/views /var/www/html/storage/framework/cache /var/www/html/storage/app/uploads
printf "ok\n" > /var/www/html/healthz.html
if [ ! -f /var/www/html/storage/.railway-setup-complete ]; then
  attempt=0
  until php artisan install --db-host="$DB_HOST" --db-port="$DB_PORT" --db-name="$DB_NAME" --db-username="$DB_USERNAME" --db-password="$DB_PASSWORD" --db-prefix="$DB_PREFIX" --company-name="$COMPANY_NAME" --company-email="$COMPANY_EMAIL" --admin-email="$ADMIN_EMAIL" --admin-password="$ADMIN_PASSWORD" --locale="$APP_LOCALE" --no-interaction; do
    attempt=$((attempt + 1))
    if [ "$attempt" -ge 12 ]; then
      exit 1
    fi
    sleep 5
  done
  touch /var/www/html/storage/.railway-setup-complete
fi
chmod -R u=rwX,g=rX,o=rX /var/www/html
chown -R www-data:root /var/www/html
exec docker-php-entrypoint apache2-foreground'`
- **Healthcheck:** `/healthz.html`
- **Networking:** Public domain with automatic HTTPS
- **Volume:** `/var/www/html`
- **Volume:** `/var/lib/mysql`

**Category:** Other

[View on Railway →](https://railway.com/deploy/akaunting)
