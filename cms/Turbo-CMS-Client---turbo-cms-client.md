# Deploy Turbo CMS Client on Railway

Ultra-fast AI CMS with e-commerce, theme builder, and high-speed blog.

[![Deploy on Railway](https://railway.com/button.svg)](https://railway.com/deploy/turbo-cms-client)

## About

Deploy **Turbo CMS** with a single click on Railway. Turbo CMS is a modern, ultra-fast Content Management System engineered for speed, high conversion, and extreme scalability. Powered by PostgreSQL 16 and Redis 7 in-memory caching, it delivers an enterprise-grade experience for blogs, high-traffic portals, landing pages, and integrated e-commerce stores.

---

Hosting Turbo CMS on Railway provides a fully managed, containerized multi-service environment without server management overhead:

- **Frontend & API Runtime**: Node.js 22 production environment with compiled Vite frontend and Express REST API, listening on port 8080.
- **Relational Persistence**: Dedicated PostgreSQL 16 database running in Railway's private network with persistent volume storage (`/var/lib/postgresql/data/pgdata`) and automated idempotent migrations on boot.
- **High-Performance In-Memory Cache**: Redis 7 cache running alongside PostgreSQL on the private network for instant page loading, session handling, and sub-millisecond query responses.
- **Media & Cloud Storage**: Seamless integration with Cloudflare R2 for unlimited media asset uploads served directly through global CDN edges.
- **Security & Reliability**: Containerized build with zero source code exposure, automatic health checks at `/api/health`, and automated restart policies.

---

## What gets deployed

| Service | Source | Type |
|---------|--------|------|
| turbo-cms | `ghcr.io/diegaosx/turbo-cms:latest` | Web service |
| Postgres | `ghcr.io/railwayapp-templates/postgres-ssl:18` | Database |
| Redis | `redis:latest` | Database |

## Environment variables

| Variable | Service | Default | Description |
| --------- | ------- | ------- | ----------- |
| `PORT` | turbo-cms | 8080 | HTTP port on which the Turbo CMS Express server listens (default: 8080). |
| `APP_URL` | turbo-cms | - | Public canonical URL of the website used for CORS, SEO sitemap and redirects. |
| `NODE_ENV` | turbo-cms | production | Application runtime environment (production). |
| `R2_BUCKET` | turbo-cms | turbo-cms-media | Target bucket name in Cloudflare R2 for file uploads. |
| `REDIS_URL` | turbo-cms | - | Redis cache connection URL automatically supplied by the Redis service. |
| `ADMIN_NAME` | turbo-cms | Administrador | Display name for the initial root administrator. |
| `JWT_SECRET` | turbo-cms | (secret) | High-entropy cryptographic secret key used to sign client JWT authentication sessions. |
| `ADMIN_EMAIL` | turbo-cms | admin@seudominio.com.br | Email address for the initial root administrator created on first migration. |
| `DB_POOL_MAX` | turbo-cms | 10 | Maximum number of concurrent connections in the PostgreSQL pool. |
| `DATABASE_SSL` | turbo-cms | false | Direct private network connection does not require SSL (false for internal network). |
| `DATABASE_URL` | turbo-cms | - | PostgreSQL connection URL automatically supplied by the Postgres service. |
| `BCRYPT_ROUNDS` | turbo-cms | 12 | Computational cost factor for bcrypt password hashing. |
| `R2_ACCOUNT_ID` | turbo-cms | - | Cloudflare Account ID for media and image storage in Cloudflare R2. |
| `R2_PUBLIC_URL` | turbo-cms | - | Public CDN URL or custom domain of the R2 bucket for serving images. |
| `UPLOAD_MAX_MB` | turbo-cms | 10 | Maximum allowed file upload size in megabytes. |
| `ADMIN_PASSWORD` | turbo-cms | (secret) | $%") }}" # Initial root administrator password automatically generated by Railway. |
| `ADMIN_USERNAME` | turbo-cms | (secret) | Username for administrative login in the CMS panel. |
| `GEMINI_API_KEY` | turbo-cms | (secret) | Google Gemini API key for native AI content generation and SEO analysis. |
| `JWT_ACCESS_TTL` | turbo-cms | 15m | User access token expiration lifespan (e.g., 15m, 1h). |
| `SEED_DEMO_DATA` | turbo-cms | false | Populate the database with demo posts and sample pages on first boot (true/false). |
| `AGENT_SERVER_URL` | turbo-cms | https://agent-mcp.saletub.com | Base URL of the dedicated AI MCP Agent server. |
| `R2_ACCESS_KEY_ID` | turbo-cms | - | Cloudflare R2 API Token Access Key ID. |
| `TURBO_MASTER_KEY` | turbo-cms | - | Secret Master Key issued by Turbo CMS Master for multi-site synchronization. |
| `TURBO_MASTER_URL` | turbo-cms | - | Base URL of the Turbo CMS Master Panel to which this site connects. |
| `CACHE_TTL_SECONDS` | turbo-cms | 60 | In-memory page and query cache duration in Redis (in seconds). |
| `LOGIN_MAX_ATTEMPTS` | turbo-cms | (secret) | Maximum allowed failed login attempts per IP before temporary lockout. |
| `LOGIN_WINDOW_MINUTES` | turbo-cms | (secret) | Time window in minutes for failed login rate limiting. |
| `R2_SECRET_ACCESS_KEY` | turbo-cms | (secret) | Cloudflare R2 API Token Secret Access Key. |
| `DEFAULT_AGENT_API_KEY` | turbo-cms | (secret) | Dedicated AI Agent API authentication key. |
| `REFRESH_TOKEN_TTL_DAYS` | turbo-cms | (secret) | Lifespan of the secure httpOnly refresh token cookie (in days). |
| `LOGIN_MAX_ATTEMPTS_PER_EMAIL` | turbo-cms | (secret) | Maximum allowed failed login attempts per email address. |
| `POSTGRES_DB` | Postgres | turbo_cms | Default database name created on startup. |
| `DATABASE_URL` | Postgres | - | Complete connection URL exposed to dependent services. |
| `POSTGRES_USER` | Postgres | (secret) | Superuser name for the PostgreSQL instance. |
| `POSTGRES_PASSWORD` | Postgres | (secret) | Strong 32-character random database password generated by Railway. |
| `REDISHOST` | Redis | - | Private Redis host domain inside Railway's internal network. |
| `REDISPORT` | Redis | 6379 | Standard listening port for the Redis cache server. |
| `REDISUSER` | Redis | default | Default Redis username for authentication. |
| `REDIS_URL` | Redis | - | Complete connection string with credentials exposed to dependent services. |
| `REDISPASSWORD` | Redis | (secret) | Password alias for Redis client compatibility. |
| `REDIS_PASSWORD` | Redis | (secret) | Strong 32-character random Redis password generated by Railway. |

## Configuration

- **Healthcheck:** `/api/health`
- **Networking:** Public domain with automatic HTTPS
- **Volume:** `/var/lib/postgresql/data`
- **Start command:** `/bin/sh -c "rm -rf $RAILWAY_VOLUME_MOUNT_PATH/lost+found/ && exec docker-entrypoint.sh redis-server --requirepass $REDIS_PASSWORD --save 60 1 --dir $RAILWAY_VOLUME_MOUNT_PATH"`
- **Volume:** `/data`

**Category:** CMS

[View on Railway →](https://railway.com/deploy/turbo-cms-client)
